// Placeholder — Phase 3 会替换为完整编排
export default function App() {
  return (
    <div className="flex items-center justify-center h-full">
      <p className="text-[var(--text-secondary)] text-sm">脚手架 OK</p>
    </div>
  )
}
